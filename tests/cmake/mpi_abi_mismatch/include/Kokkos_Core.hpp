#pragma once

// Minimal authenticated Kokkos development-contract fixture.  CMake-only smoke tests never
// compile this header; it exists so PoPS can prove that a discovered Kokkos::kokkos target carries
// the same two replayable contract headers required from a real installation.
