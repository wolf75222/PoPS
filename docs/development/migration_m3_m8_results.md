# PoPS migration results — representative qualification

**2026-09-13.** M0–M2 are preserved. M3–M8 implementation and qualification are recorded below; the remaining transport/tensor composition correction is not yet accepted. The [evidence index](migration_evidence/representative_20260913/index.json) binds each issue to actual results, source and package identities, numerical properties, and reuse decisions. The [M0–M2 record](migration_m0_m2_results.md) remains unchanged.

The user's representative-validation policy replaces repeated full matrices. Successful evidence is reused only with an explicit source-impact decision; changed or failed routes receive targeted checks. A native execution witness, a numerical qualification, and a performance measurement remain distinct claims. Failed original receipts remain failed even when a corrected successor passes.

## Implementation and evidence

| Phase | Result and representative evidence |
| --- | --- |
| M3 — fields | Independent storage, qualified stage observations, joint solve publication, and common normalization. The public three-field AMR test passes its original N16/32/64 solution/gradient oracles; a separate joint-tuple rollback and same-instance retry test passes. Retained native evidence covers signed/noncommon kernel modes, incompatibility, and normalization. |
| M4 — interactions and native calls | Typed authenticated calls and heterogeneous joint outputs preserve inventories, derivative contracts, and failure disposition. Six native solve/failure cases pass on each of two MPI ranks, covering exact, approximate, and finite-difference derivatives. Imported/native and Python laws match on two profiles. Independently compiled native components now have identical complete bytes and identities on Darwin, enabling distributed restart. |
| M5 — transport–diffusion | Public variable rotated tensors retain the N16/32/64 sequence, with observed spatial orders 1.959 and 1.990 and inventory defect below 1e-17. The two-component implicit tensor AMR solve passes actual-interface conservation, residual, and refinement checks without a second reflux. Nonidentity accumulation passes with zero energy and accumulation residual error. Fitted drift–diffusion consumes an actually solved potential at N16/32/64. **Separately authored transport/tensor composition remains under correction.** |
| M6 — solves, transactions, and time | Prepared resources and nested rollback preserve accepted state, fields, histories, and exchanges; failed native iterates cannot publish recipients. Noncommuting IMEX ARS222 retains its 8/16/32/64-step order sequence: 2.067, 2.033, 2.016. Inline and imported scalar AMR methods each complete 128 accepted steps, 129 regrids, and a bit-identical restart continuation. |
| R1 — native workflows | All eight requested workflows execute: scalar AMR advection; field-coupled transport; Euler–Poisson source; heterogeneous interaction; explicit diffusion; implicit diffusion; variable-coefficient scalar field; imported native primitive. Imported scalar AMR is an additional method witness. The Euler–Poisson example uses the declared isothermal source partition. One-step examples rely on the separately indexed numerical oracles; they do not establish convergence by themselves. |
| M7 — coupled supports and AMR | Generic physical reductions, pullbacks, and transfers retain signed weights and component/axis layouts. Seven representative transfer properties pass on each of four MPI ranks with two Kokkos OpenMP threads. Two public field/map restart variants pass on each of two ranks, alongside retained rollback evidence. |
| M8 — equivalence and retention | Fifteen actual old/new uniform-map arrays are byte-identical. Gaussian uniform/AMR comparisons preserve exact integrals, parameter rebinding, regridding, and restart; both signed far tails pass a strictly positive erfc oracle. A three-axis AMR mixture passes both parameter weights and restart paths after a checkpoint-name fixture correction. The [retention inventory](migration_m8_replacement_inventory.md) preserves supported consumers; the approved semantic deletion set is empty. |

## Provenance and measured scope

The representative record combines source-specific evidence rather than claiming one new full-suite run. The retained native gate at `13dc8bba797b42f359267422a19501af4bbb5e73` has 1,212 passes and 21 conditional skips. Public workflow and field/interaction bridges use product `77c1eb300b9a35ed018d0a31c7f7efa0a793c014`, with example/fixture fixes recorded separately. Rebuilt Dim1/2/3 packages at `3eb2722e4618d25f1586632b592ee20b4dc505fa` pass installation, byte/provenance checks, and `doctor`; their changed-route witnesses are individually indexed. The analytic mixture's later fixture source is `1e6a708423c9058041bae054d58df549f3050d5d`, with unchanged production code.

Measured MPI costs belong to one instrumented two-rank lifecycle: 705 calls per rank, 78,960 collective operand/contribution bytes, 448 send-argument bytes, 448 posted receive-capacity bytes, and process peak RSS of 24,477,696 and 22,773,760 bytes. These are logical API quantities and process-lifetime memory, not network traffic or solver-only costs. M0/M4 timings remain historical characterization; no current speedup is claimed.

Explicit tensor AMR requires a proven composite stability bound and remains refused; its synchronized implicit realization is qualified. Explicit nonlinear tensor laws, unsupported temporal affine propagation/history reconstruction, asynchronous field policies, and arbitrary coupled-system fitted fluxes retain their stated diagnostics. CPU/OpenMP/MPI evidence does not qualify GPU execution or cluster scaling. Local checks do not establish GitHub CI; PR CI is unverified.
