// GENERATED from python/bindings/seam_combinations.cmake (do not edit here; edit the manifest row).
// ADC-593 seam generation, ADC-335 (P0-B) provenance: single-block AMR seam (AmrCouplerMP) for the
// @SEAM_TRANSPORT@ transport. Instantiates only the @SEAM_TRANSPORT@ build_amr_compiled leaves. See
// amr_block_seam.hpp.
#include <pops/runtime/builders/block/amr_block_seam.hpp>

namespace pops::detail {

AmrCompiledHooks @SEAM_SYMBOL@(const ModelSpec& spec, const std::string& limiter,
                               const std::string& riemann, const AmrBuildParams& bp) {
  return build_amr_compiled_for(@SEAM_TR_CTOR@, spec, limiter, riemann, bp);
}

}  // namespace pops::detail
