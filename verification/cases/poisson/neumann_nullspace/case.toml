# Local echo of intended verification/manifest.toml [[case]] PO-03.
# Orchestrator owns the scientific manifest; this file is not registered.
id = "PO-03"
path = "verification/cases/poisson/neumann_nullspace/run.py"
name = "Neumann nullspace Poisson (1-d)"
verification_kind = "code-verification"
evidence_status = "required"
physics = ["poisson"]
oracle = "neumann_nullspace"
native_dimensions = [1]
execution_spaces = ["KokkosSerial"]
mpi_modes = ["off"]
suites = ["pr"]
requires = []

[resources.pr]
nodes = 1

[resources.two_node]
nodes = [1, 2]

[acceptance]
min_order = 1.8
