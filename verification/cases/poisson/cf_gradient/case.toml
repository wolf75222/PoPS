# Local echo of intended verification/manifest.toml [[case]] PO-06.
# Orchestrator owns the scientific manifest; this file is not registered.
id = "PO-06"
path = "verification/cases/poisson/cf_gradient/run.py"
name = "CF gradient placement (1-d)"
verification_kind = "code-verification"
evidence_status = "required"
physics = ["poisson"]
oracle = "periodic_trig"
native_dimensions = [1]
execution_spaces = ["KokkosSerial"]
mpi_modes = ["off"]
suites = ["nightly", "weekly", "release"]
requires = []

[resources.pr]
nodes = 1

[resources.two_node]
nodes = [1, 2]

[acceptance]
min_order = 1.8
placements = ["max_phi", "zero", "max_abs_dphi", "max_abs_d2phi"]
