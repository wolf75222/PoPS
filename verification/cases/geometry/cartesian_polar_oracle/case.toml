# Local echo of GE-04. Not catalogued in verification/manifest.toml (orchestrator).
id = "GE-04"
path = "verification/cases/geometry/cartesian_polar_oracle/run.py"
name = "Cartesian vs polar same radial oracle"
verification_kind = "code-verification"
evidence_status = "capability-gated"
physics = ["geometry"]
oracle = "radial_gaussian_ring"
native_dimensions = [2]
execution_spaces = ["KokkosSerial"]
mpi_modes = ["off"]
suites = ["pr"]
requires = ["polar_system_runtime"]

[polar]
branch = "capability-gated"

[resources.pr]
nodes = 1

[resources.two_node]
nodes = [1, 2]

[acceptance]
peak_radius = 0.5
field_to_field_linf_max = 0.05
n_cells = 32
